name: country_state_picker
description: A Package for Picking Countries and States.
version: 0.0.2
homepage: https://github.com/iamevansobeng/country_state_picker

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  assets:
    - lib/utils/

